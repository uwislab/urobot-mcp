# ======================================
# AI 提示词（AI Prompt）
# ======================================
ai_prompt = """
1、机器人R可以执行C程序。C程序的格式如下：

void Say_poem(){
   gpp_say(1, "桃花溪");
   gpp_say(1, "作者张旭");
   gpp_say(1, "隐隐飞桥隔野烟，石矶西畔问渔船。 桃花尽日随流水，洞在清溪何处边。");
}
Say_poem();
void main(){}
 
上述程序是用于控制机器人朗读一段诗词的，C程序中的函数定义方法与C语言一致，此外还有脚本程序的特性，在C程序最后必须包含“void main(){}”来表示这是一段需要执行的C程序。

C程序的函数调用不需要放到“main”函数中，将要调用的函数直接在程序体外调用即可。此外，C程序还可以不需要函数，直接编写一段C程序代码执行，参考如下：
 
int i = 0; 
for (i = 0; i < 3 ; i ++) 
{
    gpp_say(1, "重复的语言");
}
void main(){}

2、机器人R上支持的C语言函数包括：
2.1 机器人控制函数：
（1）void forward(int speed, int destance) 
库函数解释：用于控制机器人前进运动，包含两个参数；
参数一：int speed，说明：设定机器人前进运动的档位，范围【1-8】，效果明显档位为4，小于4的档位可能运动效果不明显；
参数二：int destance，说明：设定机器人前进运动的距离，范围在【1-200】之间。
（2）void back(int speed, int destance)
库函数解释：用于控制机器人后退运动，包含两个参数；
参数一：int speed，说明：设定机器人后退运动的档位，范围【1-8】，效果明显档位为4，小于4的档位可能运动效果不明显；
参数二：int destance，说明：设定机器人后退运动的距离，范围在【1-200】之间。
（3）void turn_left(int degree)
库函数解释：用于控制机器人左转运动，包含两个参数；
参数一：int degree，说明：机器人左转角度。
（4）void turn_right(int degree)
库函数解释：用于控制机器人右转运动，包含两个参数；
参数一：int degree，说明：机器人右转角度。
2.2 机器人语音函数
（1）void gpp_say(int mode, void *str) 
库函数解释：使机器人说话；
参数一：int mode，说明：0（当前语句打断前面语句说话），1（当前语句等待前面语句说完再说话）；
参数二：void *str，说明：说话内容，可中文课英文，字数范围20字以内。
高级用法（在参数二中加入下列标记，会有额外的发音效果）：
[h1]：字母发音方式，单词将以单个字母开始朗读；[h2]：英文发音方式，单词以其本来的发音朗读。
[m52]：男声发音；[m53]：女声发音；[m54]：唐老鸭发音；[m55]：许小宝发音。
[s?]：?为语速值，范围【0~10】。
[t?]：?为语调值，范围【0~10】。
[v?]：?为音量值，范围【0~10】。
[d]：恢复所有设置为默认设置。
[sound***]：发出指定提示音，***在【101-125】之间为信息提示音，***在【201-225】之间为铃声提示音，***在【301-330】之间为警报提示音。
高级用法示例："[sound221] 出现紧急情况，[h2]warning"。
（2）void beep(int bound, int time)
库函数解释：控制机器人蜂鸣器发出指定频率和指定时间的声音；
参数一：int bound，说明：指定发音频率，范围【200-5000】；
参数二：int time，说明：指定发音时间，范围【0-3000】，单位：ms（毫秒）。

2.3 机械手函数
（1）int servo_open(void)
库函数解释：控制打开机器人的机械手。（注意：机器人需要装配机械手，否者无效）；
返回值：-1（失败），0（成功）。
（2）int servo_close(void)
库函数解释：控制闭合机器人的机械手。（注意：机器人需要装配机械手，否者无效）；
返回值：-1（失败），0（成功）。

2.4 智能巡线控制函数
（1）int tracker_start(void)
库函数解释：开启机器人智能巡线模式；
返回值：-1（失败），0（成功）。
（2）int tracker_close(void)
库函数解释：关闭机器人智能巡线模式；
返回值：-1（失败），0（成功）。

2.5 彩色LED灯函数
void colorful_led(int mode,int rgb)
库函数解释：控制机器人炫彩LED灯亮灯模式和亮灯颜色;
参数一：int mode，说明：0（恢复两个LED灯为默认模式），1（单独控制左侧LED灯），2（单独控制右侧LED灯），3或其它数字（两个LED同时控制）;
参数二：int rgb，说明：控制LED灯亮灯的颜色，范围【1-7】。

2.6 C程序执行控制函数
（1）void Set_CScript_Mode(int mode)
库函数解释：设置机器人C程序运行模式;
参数一：int mode，说明：1（抢占式执行，直接中断前面的C程序并执行），2（等待式执行，等待前面的C程序执行完毕才执行），其它（等待式执行）.
（2）void cexit(void);
库函数解释：停止当前运行的C程序;

2.7 传感器函数
（1）int lightsensor(void);
库函数解释：获取机器人光线传感器数值;
（2）int distsensor(void);
库函数解释：障碍物距离传感器，返回机器人前面的障碍物的距离，单位：厘米;

（3）int mic1sensor(void);
库函数解释：获取机器人噪声检测传感器数值;
（4）int tracker(int id);
库函数解释：获取机器人循迹传感器数值;
参数一：int id，说明：指定循迹传感器编号，范围【1-4】，以获取该编号下的循迹传感器的状态数值；
返回值：黑线检测状态：0和1。

2.8 控制手柄函数
int Get_Ps2Value(void);
库函数解释：获取机器人控制手柄按下的按键值。

3、对机器人的编程中设计运动、发声、传感器、机械手等函数使用上面的函数来实现。
4、C程序的格式参考1中的格式来书写。
5、请使用上面的机器人C编程格式，编写一个机器人在一个房间里面通过障碍物传感器来实现漫游的程序。
"""

# ======================================
# 示例调用（可选）
# ======================================
if __name__ == "__main__":
    print("当前 AI 提示词内容：")
    print(ai_prompt)